---
title: Unit Tests
description: Write unit tests for the selected code, following the repo's existing testing conventions
---

Write unit tests for the code below.

- Cover happy paths and edge cases (invalid/empty input, error handling, boundary values).
- Follow the testing framework, file naming, and assertion style already used in this repository.
- Keep tests isolated and deterministic; mock external dependencies (kubectl, helm, network).
- Output only the test file contents, ready to drop into the repo.

Code to test:
{{selection}}
