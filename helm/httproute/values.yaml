# Default values for the httproute chart.
#
# This chart renders Kubernetes Gateway API HTTPRoute resources
# (gateway.networking.k8s.io/v1). Define one entry per HTTPRoute you want to create.
# nameOverride: ""
# fullnameOverride: ""

# The apiVersion used for all rendered HTTPRoute resources.
apiVersion: gateway.networking.k8s.io/v1

# List of HTTPRoute resources to create.
httpRoutes: []
  # - name: kargo-api-http
  #   namespace: kargo
  #   parentRefs:
  #       - name: traefik-gateway # Gateway created by Traefik when the Gateway API provider is enabled
  #         sectionName: websecure
  #         namespace: traefik
  #   hostnames:
  #     - "kargo.localhost"
  #   rules:
  #     - backendRefs:
  #       - name: kargo-api
  #         port: 80